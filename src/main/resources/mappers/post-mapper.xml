<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="postMapper">
	<resultMap type="Post" id="postResultMap">
		<id property="postNo" column="POST_NO"/>
		<result property="postWriter" column="POST_WRITER"/>
		<result property="postTitle" column="POST_TITLE"/>
		<result property="postContents" column="POST_CONTENTS"/>
		<result property="postWriter" column="POST_DATE"/>
		<result property="postDate" column="P_VIEW_COUNT"/>
	</resultMap>
	<resultMap type="Reply" id="replyResultMap">
  		<id property="replyNo" column="REPLY_NO"/>
  		<result property="postNo" column="POST_NO"/>
  		<result property="replyWriter" column="REPLY_WRITER"/>
  		<result property="replyContents" column="REPLY_CONTENTS"/>
  		<result property="replyDate" column="REPLY_DATE"/>
  	</resultMap>
  	
  	<select id="selectCount" resultType="_int">
  		SELCT COUNT(*) FROM POST
  	</select>
  	
  	<select id="selectListAll" resultMap="boardResultMap">
  		SELECT * FROM POST ORDER BY POST_NO DESC
  	</select>
</mapper>